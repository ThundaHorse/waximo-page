'use client';

import {
  FEATURES,
  FOOTER_SOCIALS,
  OPTIONS,
  OPTIONS2,
  OPTIONS3,
  OPTIONS4,
} from './constants';
import { renderPrivacyPolicy, renderTermsAndConditions } from './helpers';

export default {
  FEATURES,
  FOOTER_SOCIALS,
  OPTIONS,
  OPTIONS2,
  OPTIONS3,
  OPTIONS4,
  renderPrivacyPolicy,
  renderTermsAndConditions,
};
